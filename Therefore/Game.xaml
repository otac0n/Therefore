<Window x:Class="Therefore.Game"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Game" Height="400" Width="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="10">
            <TextBox TextChanged="TextBox_TextChanged" Tag="{Binding ElementName=Error1}" /><Label x:Name="Error1" Margin="0,0,0,5" Foreground="Red" Content="" />
            <TextBox TextChanged="TextBox_TextChanged" Tag="{Binding ElementName=Error2}" /><Label x:Name="Error2" Margin="0,0,0,5" Foreground="Red" Content="" />
            <TextBox TextChanged="TextBox_TextChanged" Tag="{Binding ElementName=Error3}" /><Label x:Name="Error3" Margin="0,0,0,5" Foreground="Red" Content="" />
            <TextBox TextChanged="TextBox_TextChanged" Tag="{Binding ElementName=Error4}" /><Label x:Name="Error4" Margin="0,0,0,5" Foreground="Red" Content="" />
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="10">
            
        </StackPanel>
    </Grid>
</Window>
